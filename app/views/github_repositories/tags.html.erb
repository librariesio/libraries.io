<% title "All #{@github_repository.full_name} tags on GitHub - Libraries" %>

<h1><%= link_to @github_repository.full_name, github_repository_path(@github_repository.owner_name, @github_repository.project_name) %> tags</h1>
<hr>
<div class="row">
  <div class="col-sm-8">
    <% if @tags.any? %>
      <ul>
        <% @tags.each do |tag| %>
        <li>
          <%= link_to tag, tag.github_url %>
          <% if tag.published_at.present? %>
          - <%= tag.published_at.to_s(:long) %>
          <% end %>
        </li>
        <% end %>
      </ul>
      <%= will_paginate @tags, page_links: false %>
    <% else %>
      <p>
        No tags found, go back to <%= link_to @github_repository.full_name, github_repository_path(@github_repository.owner_name, @github_repository.project_name) %>.
      </p>
    <% end %>
  </div>
  <div class="col-sm-4">
    <%= render 'adsense/sidebar' %>
  </div>
</div>
