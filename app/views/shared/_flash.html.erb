<%- if flash[:notice] %>
  <div class="row">
    <div class="col-md-12">
      <div class="alert alert-info" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
        <%= fa_icon 'info-circle' %>
        <%= flash[:notice] %>
      </div>
    </div>
  </div>
<%- end %>

<%- if flash[:error] %>
  <div class="row">
    <div class="col-md-12">
      <div class="alert alert-danger" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
        <%= fa_icon 'exclamation-triangle' %>
        <%= flash[:error] %>
      </div>
    </div>
  </div>
<%- end %>

<%- if flash[:success] %>
  <div class="row">
    <div class="col-md-12">
      <div class="alert alert-success" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
        <%= fa_icon 'check-circle' %>
        <%= flash[:success] %>
      </div>
    </div>
  </div>
<%- end %>

<% if current_user && current_user.token_upgrade? %>
  <div class="row">
    <div class="col-md-12">
      <div class="alert alert-info" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
        <%= fa_icon 'info-circle' %>
        We've tweaked our GitHub OAuth permissions and need you to re-authorize your account to get the best experience, thanks for beta testing Libraries.io!
        <a href="/auth/<%= current_user.private_repo_token.present? ? 'github_private' : 'github_public' %>" class="btn btn-info btn-xs learn-more" data-dismiss="alert" aria-label="Close">Re-oauthorize Now</a>
      </div>
    </div>
  </div>
<% end %>
