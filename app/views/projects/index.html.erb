<div class='row'>
  <div class='col-md-8'>
    <%= render @projects %>
    <%= will_paginate @projects, renderer: BootstrapPagination::Rails, page_links: false %>
  </div>

  <div class='col-md-4'>
    <%= render 'platforms' %>
  </div>
</div>
