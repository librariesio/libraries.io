<% if @contributors.any? %>
  <h4>Top Contributors</h4>
  <% @contributors.map(&:github_user).each do |user| %><%= link_to image_tag(user.avatar_url, width: 60, height: 60, alt: user), user_path(user) %><% end %>
<% end %>
