<% title "Repositories - Libraries.io" %>
<% description "Explore popular Repositories on Libraries.io" %>

<div class='row'>
  <div class='col-md-12'>
    <h1><i class="fa fa-<%= current_host_icon %>"></i> <%= @title %></h1>
  </div>
</div>
<hr>
<div class="row">
  <div class="col-md-8">
    <div class='row'>
      <% if @popular.any? %>
        <div class='col-sm-6'>
          <h4>
            Most Starred <%= @title %>
          </h4>
          <%= render @popular, cache: true %>
        </div>
      <% end %>
      <% if @forked.any? %>
        <div class='col-sm-6'>
          <h4>
            Most Forked <%= @title %>
          </h4>
          <%= render @forked, cache: true %>
        </div>
      <% end %>
    </div>
    <hr>
    <div class='row'>
      <% if @created.any? %>
      <div class='col-sm-6'>
        <h4>
          New <%= @title %>
        </h4>
        <%= render @created, cache: true %>
      </div>
      <% end %>
      <% if @updated.any? %>
      <div class='col-sm-6'>
        <h4>
          Recently Pushed <%= @title %>
        </h4>
        <%= render @updated, cache: true %>
      </div>
      <% end %>
    </div>
  </div>
  <div class="col-md-4">
    <%= render 'adsense/sidebar' %>
    <div class='row'>
      <% if @languages.any? %>
        <div class='col-md-12 col-sm-4 facets'>
          <h4>Popular Languages</h4>
          <ul class='filter'>
            <% @languages.each do |language| %>
              <li class='<%= 'active' if language.term.downcase == params[:language].try(:downcase) %>'>
                <% if language.term.downcase == params[:language].try(:downcase) %>
                  <%= link_to format_language(language.term), url_for(params.except(:host).merge(language: nil).permit!) %>
                <% else %>
                  <%= link_to format_language(language.term), url_for(params.except(:host).merge(language: format_language(language.term)).permit!) %>
                  <small><%= pluralize number_to_human(language['count']), 'repos' %></small>
                <% end %>
              </li>
            <% end %>
            <% if @language && !@languages.map{|l| l[0].downcase }.include?(@language.name.downcase) %>
              <li class='active'>
                <%= link_to format_language(@language.name), url_for(params.except(:host).merge(language: nil).permit!) %>
              </li>
            <% end %>
          </ul>
        </div>
      <% end %>
      <% if @licenses.any? %>
        <div class='col-md-12 col-sm-4 facets'>
          <h4>Popular Licenses</h4>
          <ul class='filter'>
            <% @licenses.each do |license| %>
              <li class='<%= 'active' if license.term.downcase == params[:license].try(:downcase) %>'>
                <% if license.term.downcase == params[:license].try(:downcase) %>
                  <%= link_to format_license(license.term), url_for(params.except(:host).merge(license: nil).permit!) %>
                <% else %>
                <%= link_to format_license(license.term), url_for(params.except(:host).merge(license: format_license(license.term)).permit!) %>
                <small><%= pluralize number_to_human(license['count']), 'repos' %></small>
                <% end %>
              </li>
            <% end %>
            <% if @license && !@licenses.map{|l| l.term.downcase }.include?(@license.id.downcase) %>
              <li class='active'>
                <%= link_to format_license(@license.id), url_for(params.except(:host).merge(license: nil).permit!) %>
              </li>
            <% end %>
          </ul>
        </div>
      <% end %>
      <% if @keywords.any? %>
        <div class='col-md-12 col-sm-4 facets'>
          <h4>Popular Keywords</h4>
          <ul class='filter'>
            <% @keywords.each do |keyword| %>
              <li class='<%= 'active' if keyword.term.downcase == params[:keywords].try(:downcase) %>'>
                <% if keyword.term.downcase == params[:keywords].try(:downcase) %>
                  <%= link_to keyword.term, url_for(params.except(:host).merge(keywords: nil).permit!) %>
                <% else %>
                  <%= link_to keyword.term, url_for(params.except(:host).merge(keywords: keyword.term).permit!) %>
                  <small><%= pluralize number_to_human(keyword['count']), 'repos' %></small>
                <% end %>
              </li>
            <% end %>
            <% if @keyword && !@keyword.map{|l| l.term.downcase }.include?(@keyword.downcase) %>
              <li class='active'>
                <%= link_to @keyword, url_for(params.except(:host).merge(keywords: nil).permit!) %>
              </li>
            <% end %>
          </ul>
        </div>
      <% end %>
    </div>
  </div>
</div>
