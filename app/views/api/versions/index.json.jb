# frozen_string_literal: true

version_attrs = %w[number published_at original_license spdx_expression].freeze

@versions.map do |version|
  version.attributes.slice(*version_attrs).merge({ coordinate: Coordinate.generate(version.project) })
end
